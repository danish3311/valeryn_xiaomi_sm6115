# Kept for reference only — not merged by the build script.
# External USB Wi-Fi and USB/UART BT dongles removed from bengal-perf_defconfig.
# Only virtual HCI remains there: CONFIG_BT_HCIVHCI=y
# Internal Wi-Fi (qcacld/cfg80211) and phone BT (BTFM_SLIM_WCN3990) are unchanged.

CONFIG_BT_HCIVHCI=y
