CONFIG_MSM_IDLE_STATS=n
CONFIG_PAGE_EXTENSION=n
CONFIG_PAGE_OWNER=n
# Required for qcacld WLAN_DEBUGFS / frame_injection debugfs entries.
# Was =n and overrode bengal-perf_defconfig, which made qdf_debugfs_init
# fail and blocked wlan driver load entirely.
CONFIG_DEBUG_FS=y
